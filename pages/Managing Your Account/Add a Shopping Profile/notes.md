### Observations for Add a Shopping Profile

- **Intent Additions**: Added `add_shopping_profile`, `edit_shopping_profile`, and `remove_shopping_profile` because they are the three concrete customer actions explicitly supported by this page.
- **Other Details**: Handled teen profile creation as a conditional system prompt action rather than a direct user-initiated intent.
