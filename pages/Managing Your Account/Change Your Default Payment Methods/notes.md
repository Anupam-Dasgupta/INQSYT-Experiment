### Observations for Change Your Default Payment Methods

- **Intent Additions**: Added `change_default_payment_method` as it is the primary customer capability described on this page.
- **Excluded Details**:
  - The warning/prompt to update existing subscriptions is treated as an integrated step of the payment change workflow rather than a standalone intent.
  - Related links (such as resolving declined payments, checking refund status, updating specific card details) are excluded as external page redirects.
